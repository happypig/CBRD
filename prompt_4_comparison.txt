你的任務要寫出完整程式碼，從作為參考的XML檔中找出所有以<ref>標記的元素，然後以<ref>內去標記後的純文字作為搜索目標參考，再分別從兩個不同的txt來源找同檔名的txt檔，並從txt內文中找出與搜索目標相似度最高的字符串，在兩相比較。由於<ref>標記字符串代表論文內所引用的佛典出處，字符串內通常包含佛教經藏名稱/冊號/經號/頁碼/欄位/行號等出處資訊，比較時特別著重在冊號/經號與頁欄行等資訊是否在OCR處理過程中有漏失，所以按字符串長度較長的TXT為遺漏較少版本。

進行步驟如下：
1) 從XML來源資料夾內建立所有XML檔的檔案清單("..\XML")
2) 每個XML檔找出所有<ref>標記，建立去除所有Tag後的純文字串清單作為搜索目標參考
3) 從兩個不同來源TXT資料夾內("..\TXT"與"..\NTXT")，找出與XML同檔名的txt檔，並以搜索目標參考為關鍵字分別找出相似度最高的字符串。字符串有可能比搜索目標參考長，因為漏字較少。
4) 將兩個不同來源的相似字符串作比較，並作統計。如果完全相同則各加1，不予列出；如果字符串不等的話(包含找不到)，請列出兩個字符串，但僅較長的版本加1。
5) 最後列出統計結果供參考